#include "libreria.h"
#include <iostream>
#include <fstream>
#include <string>

Libreria::Libreria()
{
}



using namespace std;



/*
int verificarPago(usuario usuario)// N cantidad de struc alumnos vigentes,bool
{
    if ( usuario.estado>=0)
        return 1; // cuota al dia
    else
        return -1; // usuario no realizo el pago mensual
}

bool correspondencia (usuario usuario, clases *clases, int m)
{
    clases *aux = clases+N-1;

    while( clases != aux)
    {
       if( verificaPago(usuario)==1)
        {

        }
    }
    if( verificaPago())


}
bool cupos(usuario usuario[],clases clases[],int n,int m)
{
    for(int i=0; i<n; i++){
        for(int h=0; i<m;h++){

        }
    }
}
*/
